-r requirements.txt
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
ruff>=0.4,<1.0
black>=24.0,<25.0
# Opt-in browser QA (tests/test_document_browser.py, RUN_BROWSER_TESTS=1) —
# `playwright install --with-deps chromium` also needed once, locally or in CI.
playwright>=1.45,<2.0
