__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
staticfiles/
media/
.pytest_cache/
.coverage
htmlcov/
*.sqlite3
.DS_Store
Thumbs.db
db_data/

# Local-only sample data (may contain real customer/business data) — never committed.
/sample/

# Production deployment secrets/artifacts — real values never committed.
.env.production
deploy/.env
deploy/nginx.conf
deploy/certs/
deploy/backups/

# Tailwind CSS build (CLAUDE.md's "Frontend build" section) — static/css/app.css
# is a generated artifact of assets/tailwind/input.css, never hand-edited or
# committed; `npm install && npm run build:css` (or the Docker image build)
# produces it.
node_modules/
static/css/app.css
